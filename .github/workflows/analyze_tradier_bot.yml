
name: Analyze Tradier Bot and Suggest Fixes

on:
  workflow_dispatch:

jobs:
  analyze-and-pr:
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      TRADIER_TOKEN: ${{ secrets.TRADIER_TOKEN }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run analysis script
        run: python analyze_tradier_bot.py

      - name: Clone Tradier repo privately
        run: git clone https://tbarato1804:${{ secrets.TRADIER_TOKEN }}@github.com/tbarato1804/tradier.git tradier


      - name: Create patch branch
        run: |
          cd tradier
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git checkout -b suggestion-patch-${{ github.run_id }}
          cp ../gpt_patch.py .
          git add gpt_patch.py
          git commit -m "💡 Sugerencia automática de OpenAI [skip ci]"

      - name: Push branch and open PR
        run: |
          cd tradier
          git push origin suggestion-patch-${{ github.run_id }}
          gh pr create --title "💡 Sugerencia de mejora automática"                        --body "Este PR fue generado automáticamente por el bot de revisión con OpenAI."                        --head suggestion-patch-${{ github.run_id }}                        --base main
        env:
          GH_TOKEN: ${{ secrets.TRADIER_TOKEN }}
